# Eva — worker LiveKit + dispatcher HTTP
# livekit-agents >=1.5.9 → include l'AMD (Answering Machine Detection) nel core,
# con i plugin necessari (STT/LLM/TTS/VAD/turn-detector).
livekit-agents[openai,cartesia,deepgram,silero,turn-detector]>=1.5.9,<2
python-dotenv~=1.0
aiohttp~=3.10

# Dispatcher HTTP
fastapi~=0.115
uvicorn[standard]~=0.32

# Timezone database (ZoneInfo("Europe/Rome") su immagini slim senza tzdata di sistema)
tzdata~=2024.1
